name: Release

on:
  push:
    tags:
      - "v*"

# Extract the version number from the tag (strips the leading 'v')
env:
  VERSION: ${{ github.ref_name }}

permissions:
  contents: write

jobs:
  # ── Android (all 4 ABIs) ───────────────────────────────────────────────────
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cross
        run: cargo install cross --git https://github.com/cross-rs/cross.git

      - name: Build Android arm64-v8a
        run: cross build --target aarch64-linux-android --release
        working-directory: rust

      - name: Build Android armeabi-v7a
        run: cross build --target armv7-linux-androideabi --release
        working-directory: rust

      - name: Build Android x86_64
        run: cross build --target x86_64-linux-android --release
        working-directory: rust

      - name: Build Android x86
        run: cross build --target i686-linux-android --release
        working-directory: rust

      - name: Package
        run: |
          mkdir -p artifacts
          cp rust/target/aarch64-linux-android/release/libtypst_flutter.so \
             artifacts/libtypst_flutter_android_arm64.so
          cp rust/target/armv7-linux-androideabi/release/libtypst_flutter.so \
             artifacts/libtypst_flutter_android_armv7.so
          cp rust/target/x86_64-linux-android/release/libtypst_flutter.so \
             artifacts/libtypst_flutter_android_x64.so
          cp rust/target/i686-linux-android/release/libtypst_flutter.so \
             artifacts/libtypst_flutter_android_x86.so

      - uses: actions/upload-artifact@v4
        with:
          name: android-libs
          path: artifacts/

      - uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── iOS (fat static lib: device arm64 + sim arm64 + sim x86_64) ──────────
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Add iOS targets
        run: |
          rustup target add aarch64-apple-ios
          rustup target add aarch64-apple-ios-sim
          rustup target add x86_64-apple-ios

      - name: Build iOS device (arm64)
        run: cargo build --target aarch64-apple-ios --release
        working-directory: rust

      - name: Build iOS simulator (arm64)
        run: cargo build --target aarch64-apple-ios-sim --release
        working-directory: rust

      - name: Build iOS simulator (x86_64)
        run: cargo build --target x86_64-apple-ios --release
        working-directory: rust

      - name: Create iOS XCFramework
        run: |
          # Combine both simulator architectures into a single fat library
          lipo -create \
            rust/target/aarch64-apple-ios-sim/release/libtypst_flutter.a \
            rust/target/x86_64-apple-ios/release/libtypst_flutter.a \
            -output rust/target/libtypst_flutter_sim_fat.a

          # Create the xcframework from the device arm64 and the simulator fat lib
          xcodebuild -create-xcframework \
            -library rust/target/aarch64-apple-ios/release/libtypst_flutter.a \
            -library rust/target/libtypst_flutter_sim_fat.a \
            -output typst_flutter.xcframework

      - name: Zip XCFramework
        run: zip -r libtypst_flutter_ios.xcframework.zip typst_flutter.xcframework

      - uses: actions/upload-artifact@v4
        with:
          name: ios-lib
          path: libtypst_flutter_ios.xcframework.zip

      - uses: softprops/action-gh-release@v1
        with:
          files: libtypst_flutter_ios.xcframework.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── Linux x86_64 ──────────────────────────────────────────────────────────
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Build Linux x86_64
        run: cargo build --target x86_64-unknown-linux-gnu --release
        working-directory: rust

      - name: Package
        run: |
          mkdir -p artifacts
          cp rust/target/x86_64-unknown-linux-gnu/release/libtypst_flutter.so \
             artifacts/libtypst_flutter_linux_x64.so

      - uses: actions/upload-artifact@v4
        with:
          name: linux-lib
          path: artifacts/

      - uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── Windows x86_64 ────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Build Windows x86_64
        run: cargo build --target x86_64-pc-windows-msvc --release
        working-directory: rust

      - name: Package
        run: |
          New-Item -ItemType Directory -Force -Path artifacts | Out-Null
          Copy-Item rust\target\x86_64-pc-windows-msvc\release\typst_flutter.dll `
                    artifacts\typst_flutter_windows_x64.dll

      - uses: actions/upload-artifact@v4
        with:
          name: windows-lib
          path: artifacts/

      - uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── macOS universal (arm64 + x86_64) ────────────────────────────────────
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Add macOS targets
        run: |
          rustup target add x86_64-apple-darwin
          rustup target add aarch64-apple-darwin

      - name: Build macOS x86_64
        run: cargo build --target x86_64-apple-darwin --release
        working-directory: rust

      - name: Build macOS arm64
        run: cargo build --target aarch64-apple-darwin --release
        working-directory: rust

      - name: Create universal dylib
        run: |
          lipo -create \
            rust/target/x86_64-apple-darwin/release/libtypst_flutter.dylib \
            rust/target/aarch64-apple-darwin/release/libtypst_flutter.dylib \
            -output libtypst_flutter_macos_universal.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: macos-lib
          path: libtypst_flutter_macos_universal.dylib

      - uses: softprops/action-gh-release@v1
        with:
          files: libtypst_flutter_macos_universal.dylib
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── SHA-256 checksums ─────────────────────────────────────────────────────
  generate-checksums:
    needs: [build-android, build-ios, build-linux, build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: all-artifacts
          merge-multiple: true

      - name: Generate SHA256SUMS
        run: |
          cd all-artifacts
          sha256sum * > ../SHA256SUMS
          cat ../SHA256SUMS

      - uses: softprops/action-gh-release@v1
        with:
          files: SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── pub.dev publish ───────────────────────────────────────────────────────
  publish-dart:
    needs: [generate-checksums]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"

      - name: Install dependencies
        run: flutter pub get

      - name: Setup pub credentials
        run: |
          mkdir -p ~/.config/dart
          echo '${{ secrets.PUB_CREDENTIALS }}' > ~/.config/dart/pub-credentials.json

      - name: Validate package (dry run)
        run: flutter pub publish --dry-run

      - name: Publish to pub.dev
        run: dart pub publish --force
